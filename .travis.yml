version: ~> 1.0
dist: focal
language: python
python:
  - "3.7"
  # - "3.8"
  # - "3.9"
  # - "3.10"
install:
  - which pip
  - pip install --upgrade pip 
  - pip --version
  - pip install poetry
  - travis_retry poetry install
script:
   - make lint
   - pytest tests/test_local*.py
branches:
  only:
  - main
env:
  - PIP_DEFAULT_TIMEOUT=100

